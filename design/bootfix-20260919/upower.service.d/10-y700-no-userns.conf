# y700: the Android GKI kernel has no CONFIG_USER_NS; PrivateUsers=yes makes upowerd fail with 217/USER.
# Only this one sandbox option is turned off; the other hardening options (mount namespace based) stay.
[Service]
PrivateUsers=no
